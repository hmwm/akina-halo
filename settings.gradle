pluginManagement {
    repositories {
        maven { url = 'https://maven.aliyun.com/repository/public' }
        gradlePluginPortal()
    }
}

rootProject.name = 'akina'
include 'api', 'application', 'platform:application', 'platform:plugin', 'ui'
